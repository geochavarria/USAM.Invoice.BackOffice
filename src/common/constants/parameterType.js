/**
 * @module Parameter_Constant
 */ 
const STRING = { code : "S", label : "Caracteres" }
const NUMBER = { code : "N", label : "Numérico" }
const BOOLEAN = { code : "B", label : "Lógico" }
export const ParamType = {
    STRING,
    NUMBER,
    BOOLEAN,
}

export const ParamTypeList = [
    STRING,
    NUMBER,
    BOOLEAN
]